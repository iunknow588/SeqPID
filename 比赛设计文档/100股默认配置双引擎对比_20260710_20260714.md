# 100股默认配置双引擎对比

日期：2026-07-14  
数据交易日：20260710  
比较对象：Python 比赛系统 vs Rust 比赛系统  
运行入口：`E:\2026OPC大赛\自动化交易\scripts\work\run_ana.ps1`

---

## 1. 运行配置

两次运行均使用默认配置文件：

- Python：`自动化交易/比赛系统/configs/dev.yaml`
- Rust：`自动化交易/src-rust/configs/dev.yaml`

共同运行参数：

- `TradeDate = 20260710`
- `InputDir = C:\level-2-ana\data\20260710\20260710`
- `StockListFile = auto -> C:\level-2-ana\data\百只股票样本.csv`
- 股票清单规模：100
- 原始有效样本：97
- 补全样本：3
  - `688819.SH`
  - `688717.SH`
  - `688687.SH`

输出目录：

- Python（修复后复跑）：
  - `E:\2026OPC大赛\自动化交易\output_compare\python_after_fix\20260710_20260714_113739`
- Rust：
  - `E:\2026OPC大赛\自动化交易\output_compare\rust\20260710_20260714_112031`

---

## 2. 主要结论

### 2.1 正式提交结果已对齐

以下两份正式提交文件，经按 `stock_code + transaction_date` 排序后比较，结果一致：

- `predict_result.csv`
- `pattern_reco.csv`

结论：

- `predict_result.csv`：100/100 行完全一致
- `pattern_reco.csv`：100/100 行完全一致
- `submit.zip` 内部解压后两份正式 CSV 语义一致

### 2.2 标签分布已对齐

`label_distribution.csv` 一致，核心统计如下：

- `pattern_type`
  - `对倒拉升 = 35`
  - `盘中诱多 = 18`
  - `尾盘突袭 = 16`
  - `日内套利 = 13`
  - `连续小单推升 = 10`
  - `大单吸筹 = 7`
  - `分时脉冲 = 1`
- `capital_type`
  - `量化 = 68`
  - `游资 = 32`
- `capital_intention`
  - `卖出 = 52`
  - `买入 = 45`
  - `中性 = 3`

### 2.3 市场快照已对齐

`market_pid_snapshot.csv` 的核心业务值一致。个别字段文本格式略有不同，例如：

- Python：`3.5e-05`
- Rust：`0.000035`

但数值一致，属于表示层差异。

### 2.4 窗口级 PID 诊断数值已对齐

`pid_window_diag.csv` 共 4656 行。直接按字符串比较时有 1496 处差异，但转为浮点后比较：

- `y_observed`
- `y_hat_next`
- `capital_mix`
- `param_stability_flag`

结果为：**数值完全一致**。

这部分差异来自：

- `0` vs `0.0`
- `7e-06` vs `0.000007`
- 类似的科学计数法/定点小数写法差异

### 2.5 日级 PID 诊断已基本对齐

在修复 Python 导出 `warnings` 字段后，`pid_daily_diag.csv` 与 Rust 对比结果为：

- `mode_name` 一致
- `param_stability_flag` 一致
- `m_eff_uncertainty_flag` 一致
- `m_eff_rank_eligible` 一致
- `data_leakage_check` 一致
- `sample_origin` 一致
- `reason_code` 一致
- `warnings` 一致

---

## 3. 本轮修复

本轮比较过程中发现并修复了一个 Python 导出问题：

- 文件：`自动化交易/比赛系统/src/exporter.py`
- 问题：`export_pid_daily_diag_records()` 在 `warnings` 为字符串时，错误地执行 `list("KF did not converge")`，导致导出内容变成：
  - `K | F |   | d | i | d | ...`
- 修复：字符串警告改为单元素列表处理，不再按字符拆分

修复后已执行：

- `pytest tests/unit/test_exporter.py`：通过
- Python 100 股默认配置复跑：完成

---

## 4. 性能对比

### Python

- `total_seconds = 338.531347`
- `sample_build_seconds = 335.171211`
- 最慢样本：
  - `600176.SH = 33.251307s`

### Rust

- `total_seconds = 209.889906`
- `sample_build_seconds = 209.689714`

### 结论

在同一批 100 股、同一默认配置、同一交易日数据下：

- Rust 总耗时明显低于 Python
- 当前主要耗时仍集中在样本构建阶段

---

## 5. 仍存在的非业务性差异

以下差异暂不影响正式提交结果：

1. `batch_diagnostics.json`
   - 键顺序不同
   - 浮点显示精度略有不同
2. `market_regime_report.md`
   - 文本格式不同
3. `100_stock_replay_report.md`
   - 文本组织方式不同
4. `performance_profile.json`
   - 字段结构不同
   - Python 含 `top_slowest_samples.stock_code`
   - Rust 当前 `top_slowest` 仅保留时长，缺少股票代码

这些差异属于报告和表示层差异，不影响主输出结论，但后续可以继续统一。

---

## 6. 当前未完成项

`m_eff / mv_ratio` 仍未形成正式计算闭环。

当前状态是：

- 文档已定义正式口径
- Python / Rust 导出层已有字段占位
- 调度层已有默认标记
- 但尚未完成真正的 `beta_norm / m_eff / uncertainty / rank_eligible` 计算实现

因此这部分目前只能视为：

- **接口和文档边界已建立**
- **核心算法实现仍待补齐**

---

## 7. 综合结论

截至 2026-07-14，本次用默认配置对 `20260710` 的 100 股样本执行双引擎实跑后，可以得出：

1. Python 与 Rust 在正式提交口径上已经对齐。
2. 4D `capital_mix` 主口径、mixed-pool 分类边界、状态特征边界在两边均已落地。
3. 窗口级与日级 PID 诊断的核心业务字段已经对齐，剩余差异主要是文本格式和序列化方式。
4. Rust 在当前 100 股样本上的运行性能明显优于 Python。
5. 下一阶段最应该推进的是 `m_eff / mv_ratio` 的正式计算闭环，而不是继续纠缠已经不影响主输出的表示层差异。
---

## 8. 2026-07-14 补充更新：`m_eff` 诊断代理已落地

在本轮代码对齐后，Python 与 Rust 已新增诊断专用的 `beta_norm / m_eff` 代理输出，并再次使用默认配置完成 100 股样本实跑：

- Python：
  - `E:\2026OPC大赛\自动化交易\output_compare\python_proxy_diag\20260710_20260714_125357`
- Rust：
  - `E:\2026OPC大赛\自动化交易\output_compare\rust_proxy_diag\20260710_20260714_124550`

本轮新增实现遵循文档边界：

1. 仍没有把 `m_eff` 升级为正式 `mv_ratio` 口径结果。
2. 新增结果只进入 `pid_window_diag.csv / pid_daily_diag.csv` 诊断链路。
3. 统一输出：
   - `beta_norm_unit = amount_response`
   - `m_eff_source_type = amount_ratio_proxy`
   - `m_eff_rank_eligible = false`
4. 若代理值触发下限截断，则写入 `m_eff_clipped_flag = true`；否则保持 `false`。

本轮双引擎对比结论：

- `predict_result.csv`：逐行一致
- `pattern_reco.csv`：逐行一致
- `label_distribution.csv`：逐行一致
- `market_pid_snapshot.csv`：业务数值一致，差异仍为科学计数法/定点表示
- `pid_daily_diag.csv`：新增代理来源字段已对齐，差异仅为 `1.0` vs `1`、`10.0` vs `10` 这类格式化差异
- `pid_window_diag.csv`：新增代理列数值对齐，差异主要为 `-0.0` vs `-0`、`1000000.0` vs `1000000` 这类表示层差异

因此，当前状态应修正为：

- 正式 `m_eff / mv_ratio` 闭环：仍未完成
- 诊断代理版 `m_eff`：已在 Python / Rust 双端落地，并完成 100 股默认配置实跑对齐
